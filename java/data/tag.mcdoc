dispatch minecraft:resource[tag] to struct Tag {
	replace?: boolean,
	values: [TagEntry],
}

type TagEntry = (
	#[id(registry="%tag",tags="allowed")] string |
	#[since="1.16.2"]
	struct {
		id: #[id(registry="%tag",tags="allowed")] string,
		required?: boolean,
	} |
)
