#[since="1.19"]
dispatch minecraft:entity[tadpole] to struct Tadpole {
	...super::MobBase,
	/// Age of it in ticks. When greater than or equal to 24000, it grows into a frog.
	Age?: int,
	/// If it was released from a bucket.
	FromBucket?: boolean,
}
